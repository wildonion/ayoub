#!/bin/bash
set -e RUSTFLAGS='-C link-args=-s' rustup target add wasm32-unknown-unknown && cargo build --target wasm32-unknown-unknown --release && mkdir -p out && cp target/wasm32-unknown-unknown/release/*.wasm out/bluerangene.wasm