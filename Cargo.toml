[workspace]

members = [
    "PaaS/src/wasi/",
    "PaaS/src/wasi/src/client/",
    "PaaS",
    "coiniXerr",
]


[profile.release]
panic = "abort"
opt-level = "z"
lto = true
codegen-units = 1
overflow-checks = true # panic on integer overflow